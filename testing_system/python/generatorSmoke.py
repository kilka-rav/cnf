import random as rd
 
file = open("testData.h", 'w')
numTest = 32
clauses = 100
maxVar = 12
numVar = 12

def gen(val):
    a = rd.randint(-val, val)
    if a == 0:
        return 1
    return a

file.write("//this file was autogenerated\n")

def writeFile(file, maxVarGlob, numVar, i, globClauses):
    maxVar = rd.randint(2, maxVarGlob)
    clauses = rd.randint(2, globClauses)
    line = "constexpr int size" + str(i) + " = " + str(clauses) + ";"
    file.write(line)
    line = "\nconstexpr std::array<std::array<int, 3>, " + str(clauses) + "> input" + str(i) + " = {{"
    for i in range(clauses):
        if i == clauses - 1:
            line = line + "{" + str(gen(maxVar)) + ", " + str(gen(maxVar)) + ", " + str(gen(maxVar)) + "}}};\n"
        else:
            line = line + "{" + str(gen(maxVar)) + ", " + str(gen(maxVar)) + ", " + str(gen(maxVar)) + "}, "
    file.write(line)
    
i = 2
while i <= numTest:
    writeFile(file, maxVar, numVar, i, clauses)
    i = i + 1

file.close()